interface:
  display_name: "GPT Pro + Sol Advisor"
  short_description: "Luna-first implementation with Terra/Sol escalation"
  default_prompt: "Use $orchestrate-gpt-pro-sol-advisor for explicit combined mode: Luna/Max implementation and small-review sub-agent, optional one-pass read-only Sol high-impact consultation, Terra/High escalation when needed, and one final GPT Pro semantic review."
policy:
  allow_implicit_invocation: false
