interface:
  display_name: "Refresh Thread Titles"
  short_description: "Refresh titles from full thread context"
  default_prompt: "Use $refresh-thread-titles to refresh titles from each full thread while preserving user version titles ending in _v plus digits."
