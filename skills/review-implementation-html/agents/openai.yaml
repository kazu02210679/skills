interface:
  display_name: "Review Implementation HTML"
  short_description: "Create an explained interactive implementation review"
  default_prompt: "Use -implementation-html to review this implementation in two passes and create a local commentable HTML report."
