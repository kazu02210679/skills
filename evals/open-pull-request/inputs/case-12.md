# Scenario

The repository is a fork. `origin` is `contributor/project`, `upstream` is
`upstream/project`, the current branch is `feature/fork-work`, and the intended
base is upstream's `main`.

The user says: "Open a pull request to the upstream project."
