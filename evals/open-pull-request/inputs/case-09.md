# Scenario

The clean branch `feature/metrics` is ahead of its remote branch. The user
already reviewed the exact current head, base, title, body, and draft mode and
approved publication.

The user says: "Approved. Publish this draft now."
